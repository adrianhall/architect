# Bugs from Issue 12

## Bug 1: On the dashboard, the CTA is too narrow

The CTA appears as

```text
Start
designing
your
Cloudflare
architecture.
Create
 a
blank
diagram
or
explore
blueprints
for
inspiration.
```

... all stacked on top of each other.  The block starts 50% across the view port with the left edge under the center of "Create your first diagram" and the "New Diagram" button"

## Bug 2: Duplicate New Diagram buttons

When there are no cards, the New Diagram is in the CTA block and at the top-right of the screen (in line with the title).

## Bug 3: Deleting opens editor for the diagram instead

Not sure if it's the same diagram.  I selected triple-dot and delete on the LAST diagram and it opened the diagram editor instead.
